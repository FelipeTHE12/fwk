import { calcularNumerosPrimos } from "../calcularNumerosPrimos";

describe("Calcular Numeros Primos", () => {
  test("deve retornar hello", () => {
    const a = "hello";

    expect(a).toBe("hello");
  });
});
